doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous")
        script(src="https://cdn.jsdelivr.net/npm/normalizr@3.6.1/dist/normalizr.browser.min.js")
        title Formulario 
    body
        div(class="container mt-3")
            div(class="jumbotron")
                h1(style="color: blue;") Login de usuario
                br
                form(action="/login" method="post" role="form" autocomplete="off")
                    div(class="form-group")
                        label(for="username")
                            b Ingrese su correo electronico
                        input(id="username" placeholder="email" class="form-control" name="username" type="email" required)
                    div(class="form-group")
                        label(for="password")
                            b Ingrese su password
                        input(id="password" name='password' placeholder="password" class="form-control" type="password" required)
                    button(class="btn btn-success mt-3 mb-5 btn-block" type='submit') Enviar
            hr
            div(class="text-left")
                button(class="btn btn-success my-3" onclick="location.href = '/registro'") Ir a Register
